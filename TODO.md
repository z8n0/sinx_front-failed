# TO-DO List
## Introduction (**It's important to read this section if it's your first time!**)
in this file we gonna store all tasks that we need to do in this project.
if you want to edit this file, at first please talk to the project owners (now, A-Zero and ruxa are the owners), then write your task here. and if you mind to do one of these tasks, To start, you have to edit this file and assign the task that shows you are working on it and Then, create a branch for that (e.g `features/admin_panel`) and then do your task there and create a pull request and wait until we check it.
There are 3 kind of tasks here, **Feature**, **Fix** and **Refactor**.<br>
- **Feature** means that this task adds a new thing to the project.<br>
- **Fix** means that there is a bug that we have to fix.<br>
- **Refactor** means that they're some codes in the project that we have to change something in it or write it again.<br>

### Example
- [ ] **Some Task** => explain it... || (who is working on it or did it!) (todo, in progress, did)

<hr>

## Features
- [ ] **Posts Panel** => To allow users to create their posts, we have to create a panel or section that the user and create, delete and manage their posts there. I don't have any idea "Where should we put it" or "How should design be?" but I just know we need it! || (?) (todo)

## Fixes

## Refactorings
- [ ] **Writing some ready-to-use css classes** => I think there may be better that we have some ready-to-use classes that prevents using a style in each classes and I could make the development more faster. some classes e.g. `btn`, `btn-primary`, `flex`, `color-{color}`, `bg-{color}`, `m-{x}`, `p-{x}` and etc. || (?) (todo)